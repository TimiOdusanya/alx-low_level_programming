#include "main.h"

/**
 * _abs - Absolute value of an int n
 * @n: Operand 1
 * Description: ABsolute value of an int
 *
 * Return: Always n
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n == 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
