#include "main.h"

/**
 * _islower - Checks for lowercase characters
 *@c: Operand 1
 * Description: Checks for lowercase characters
 *
 * Return: Always 0
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
